{
  "name": "root",
  "private": true,
  "version": "0.0.0-dev.18",
  "scripts": {
    "publish": "lerna publish --no-git-reset --no-git-tag-version --no-commit-hooks --no-push --yes",
    "postinstall": "lerna bootstrap",
    "build": "lerna run build"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
