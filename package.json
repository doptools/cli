{
  "name": "root",
  "private": true,
  "version": "0.0.0-dev.20",
  "scripts": {
    "publish": "lerna publish --no-git-tag-version --no-commit-hooks --no-push --force-publish --yes",
    "postinstall": "lerna bootstrap",
    "build": "lerna exec yarn build"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
